version: "3.9"

services:
  rag-api:

    build: .

    container_name: opkey-rag-agent

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - chroma_data:/app/chroma_db
      - ./data:/app/data

volumes:
  chroma_data: